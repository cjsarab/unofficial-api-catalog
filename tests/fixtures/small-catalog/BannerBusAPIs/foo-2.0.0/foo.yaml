openapi: 3.0.0
info:
  title: Foo API
  description: A sample API for testing the indexer — v2.
  version: "2.0.0"
  x-source-system: banner
  x-source-domain: Student
  x-source-title: Foo
  x-api-type: other
  x-release-status: beta
paths:
  /api/foo:
    get:
      summary: Get foo records
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    x-lineageReferenceObject: SPRIDEN_ID
